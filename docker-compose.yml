version: '3'
services:
  ros2_humble_gazebo:
    build:
      context: ./cpu_central/
      dockerfile: Dockerfile
    container_name: robot_cpu_central
    network_mode: "host"
    environment:
      DISPLAY: ${DISPLAY}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./ros2_humble_ws:/ros2_humble_ws